#include <stdio.h>
#include <math.h>
double sum(double x1, double y1, double x2, double y2);
int main() {
    double x1, y1, x2, y2;
    scanf("%ld")
    return 0;
}
double sum(double x1, double y1, double x2, double y2) {
    double temp = sqrt(pow(fabs(x2 - x1), 2) + pow(fabs(y2 - y1), 2)) 
    return temp;
}